# Data Plane Reporting
markdown>=3.4.0

# Data manipulation
pandas>=2.1.0
numpy>=1.26.0

# AWS S3
boto3>=1.34.0
botocore>=1.34.0

# YAML
PyYAML>=6.0.0

# SQLite (stdlib) — no extra package needed

# OpenTelemetry — metrics + traces
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-prometheus>=0.45b0
opentelemetry-exporter-otlp>=1.24.0
prometheus-client>=0.20.0

# HTTP / dashboard
fastapi>=0.110.0
uvicorn>=0.29.0
jinja2>=3.1.0

# Scheduling
schedule>=1.2.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
moto[s3]>=5.0.0       # S3 mocking for tests

# Utilities
python-dotenv>=1.0.0
tabulate>=0.9.0        # markdown table output in notebook
openai>=1.14.0
