---
title: "OpenClaw Findings"
layout: default
---

# OpenClaw — Vulnerability Investigation Findings

> Automated exploitation investigation reports generated by **OpenClaw**.
> Run the harness to populate this page.

---

## No findings yet

Run the harness to generate investigation reports:

```bash
# Local telemetry (dev mode)
python -m harness.orchestrator --raw-telemetry local --once

# S3 telemetry (production mode)  
python -m harness.orchestrator --raw-telemetry s3 --once
```

This page will be automatically rebuilt after each investigation cycle.
